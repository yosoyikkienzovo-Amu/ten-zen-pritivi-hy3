# HANDOFF — Estado del proyecto MemPalace + OpenCode/Ollama al 2026-07-16

Este archivo es el punto de entrada para retomar el proyecto en un chat nuevo. Léelo entero antes de tocar nada.

## 1. Qué es este proyecto
Ikki está montando un stack de IA local ("Mutaitomajunia") en `~/ten-zen-pritivi-hy3`, con MemPalace como sistema de memoria persistente. Estilo de trabajo: directo, técnico, sin adulación, todo enumerado en orden ascendente, sin narrar el proceso de pensamiento antes de responder.

## 2. Máquina real (confirmado por comando, no asumido)
- Pop!_OS 24.04 LTS
- CPU: Intel Haswell-ULT integrado, 4 núcleos — **sin GPU dedicada** (corrige cualquier referencia previa a "NVIDIA desktop")
- RAM: 15.5 GB total
- Acceso: Desktop Commander (MCP) da filesystem real de `/home/ikki` — puede desconectarse a mitad de sesión, hay que verificar antes de asumir que está activo

## 3. Estado de MemPalace — LO MÁS IMPORTANTE
- Instalado: v3.5.0, binario en `~/.local/bin/mempalace` (pipx)
- **`~/.mempalace/` NO EXISTE TODAVÍA** — cero init, cero mining, cero config real. Todo lo hecho hasta ahora es lectura de documentación y preparación, no ejecución.
- Documentación oficial (mempalaceofficial.com) leída y resumida COMPLETA (18 páginas: Guide + Concepts + Reference relevantes) en `Mem Palace para Agentes/` (archivos 01-09) y `Mem Palace para Agentes/Palacio de la Memoria Amú Judár/` (00-03) dentro del repo.
- **Repo:** `~/ten-zen-pritivi-hy3`, todo el trabajo de documentación está en la rama `mempalace-docs` (NO mergeada a `master` — decisión pendiente del usuario).

### 3.1 Acción de mayor prioridad, sin importar qué más se haga
Claude Code borra transcripts JSONL a los 30 días sin hooks de auto-save. Primer comando real a correr cuando se decida activar MemPalace:
```
mempalace mine ~/.claude/projects/ --mode convos --wing claude_imports
```

### 3.2 Checklist de configuración pendiente, en orden
1. `mempalace init ~/ten-zen-pritivi-hy3` (proyecto ancla) — y decidir si también `~/agents`
2. Backfill de Claude Code (3.1) + mining normal de proyectos, con `--wing` separado por proyecto/persona (nunca minar todo junto)
3. Wirear hooks `Stop` (cada 15 msj) + `PreCompact` en `~/.claude/settings.json`, reiniciar Claude Code después
4. `mempalace mcp` para generar el comando de conexión MCP a Claude Desktop/Code
5. Backend: dejar ChromaDB default. NO tocar Qdrant/pgvector/Milvus salvo decisión explícita (mandan datos fuera de la máquina si se configuran mal)
6. **NO activar `mempalace compress`/AAAK** — confirmado con fuente oficial: 12.4 puntos de regresión de recall (96.6%→84.2%), experimental, pensado para miles de sesiones, no para setup inicial
7. `mempalace status` para verificar

### 3.3 Cosas que NO existen todavía en MemPalace — no las busques
- `mempalace_list_agents` / registro de agentes especializados — NO implementado (confirmado con doc oficial). Lo que sí existe: diario simple por `agent_name` fijo vía `mempalace_diary_write`/`_read`.
- Contradiction detection — admitido como roadmap por la propia doc, no funcional, solo primitivas del knowledge graph disponibles.
- Comandos `daemon`, `migrate`, `palace` (aparecen en `--help` del binario real) — sin documentación oficial encontrada, probar con cautela antes de usar.

### 3.4 Sobre las cifras de benchmark (por si se repite la duda)
96.6% = recall crudo verbatim, cero LLM, cifra de referencia real. 98.4% = con boosting adicional (sigue sin LLM). El proyecto descartó un "100%" anterior por no generalizar — transparencia real de su parte, no hay nada turbio en la fuente oficial. Sí hay sitios impostores (`.tech`, `.net`, `memplace.app` con historia falsa de una actriz) — únicas fuentes válidas: `mempalaceofficial.com`, `github.com/MemPalace/mempalace`, PyPI.

## 4. Stack OpenCode + Ollama — estado real
- **OpenCode:** v1.17.20, dos configs: `~/.config/opencode/opencode.json` (global, YA ACTUALIZADA con provider Ollama) y `~/agents/opencode/opencode.json` (config de proyecto, carpeta de respaldo, con modelos pagos, no editada, no se usa activamente)
- **Ollama:** v0.31.2, servicio systemd activo. Modelos registrados: `llama3.1:latest` (4.9GB), `llama2:latest` (3.8GB), `all-minilm:latest` (45MB, SOLO embeddings, no chat), `tinyllama:latest` (637MB)
- **`/home/ikki/Modelos/` — 4 GGUF sin importar a Ollama todavía:** `Hunyuan-MT-7B-q4_k_m.gguf`, `Qwen3.5-4B-GGUF`, `Qwythos-9B-v2-MTP-Q4_K_M.gguf` (5.9GB), `gemma4-coding-Q4_K_M.gguf` (7.3GB). Ninguno usable hasta `ollama create <nombre> -f Modelfile`. **Ojo con RAM:** `gemma4-coding` solo (7.3GB) deja poquísimo margen en una máquina de 15.5GB sin GPU — decidir con cautela antes de cargarlo.
- **`opencode.json` global ya tiene:** provider `ollama` (baseURL `http://127.0.0.1:11434/v1`, modelos `llama3.1`/`llama2`/`tinyllama` declarados explícitamente — evita el bug conocido de OpenCode donde Ollama aparece conectado pero sin modelos listados) + provider `openrouter` (vía `{env:OPENROUTER_API_KEY}`) + `default_agent: "plan"` (modo lectura por defecto, seguridad).
- **`.env` real en `~/.config/opencode/.env`** (permisos 600, sourced desde `~/.bashrc` con `set -a; source ...; set +a`) con ~19 API keys que el usuario pegó. **Ninguna key vive en el JSON** — todo vía `{env:VAR}`.
- **Expediente completo:** `docs/OPENCODE-OLLAMA-EXPEDIENTE.md` en el repo.

### 4.1 Pendiente en OpenCode+Ollama
1. Probar `opencode run "hola"` con el config real, confirmar que responde.
2. Decidir si importar alguno de los 4 GGUF de `/home/ikki/Modelos/` a Ollama (evaluar RAM antes, uno a la vez).
3. Conectar MemPalace: usar la vía manual de inyección de contexto (Local Models, ver `09-claude-code-hooks-localmodels-benchmarks.md` sección 4) — `search_memories()` → formatear → meter en el prompt, ya que Ollama no habla MCP nativo.

## 5. Reglas duras que no cambian entre sesiones
1. Nunca `git add -A` sin revisar antes — este repo tiene archivos de otros procesos/instancias sueltos que no hay que arrastrar a los commits (pasó dos veces: `cerebro_core.py`, `quickstart-summary/`).
2. Nunca API keys ni tokens en texto plano dentro de archivos versionados — siempre `{env:VAR}` o similar, `.env` con 600 y gitignored.
3. Código listo para copiar/pegar sin comentarios inline — las aclaraciones van afuera del bloque de código, nunca adentro.
4. Enumerar todo en orden ascendente.
5. No usar `visualize`/widgets para contenido de texto plano que el usuario va a copiar — eso rompe el flujo, se pidió corregir una vez ya.
6. Verificar con comandos reales antes de afirmar cualquier dato de hardware/software — ya hubo una corrección real (GPU NVIDIA que no existe).

## 6. Qué preguntar al arrancar el chat nuevo
No hace falta preguntar nada para arrancar — este archivo tiene todo. Si hay ambigüedad real sobre por dónde seguir, las opciones son (en orden de prioridad real): (a) ejecutar la checklist 3.2 de MemPalace, (b) probar y rematar el setup de OpenCode+Ollama (4.1), (c) decidir si mergear `mempalace-docs` a `master`.
