# código-n8n-javascript

Guía de expertos para escribir código JavaScript en nodos de código n8n.--- ```javascript // Plantilla básica para nodos de código const items = $input.all();// Procesar datos constantes procesados ​​= items.map(item => ({ json: {