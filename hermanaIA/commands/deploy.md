---
description: Build and package
agent: build
---
Build and package the project for deployment.

1. Run the build command for the project
2. Verify the build output is complete and correct
3. Report the build summary including output size and any warnings
4. If a Dockerfile exists, verify it builds too
